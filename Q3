from datetime import datetime
def contains_duplicates(elements):
    for i in range(0,len(elements)):
        for j in range (0,len(elements)):
            if i ==j:
                continue
            if elements[i] == elements[j]:
  #              return True
                 return elements[i] 
    return False

now = datetime.now()
print (str(now))
result = contains_duplicates([10,0,5,3,-19,5])
print (str(result))
now = datetime.now()
print (str(now))


from datetime import datetime
def contains_duplicates(elements):
    for i in range(0,len(elements)):
        for j in range (0,len(elements)):
            if i ==j:
                continue
            if elements[i] == elements[j]:
  #              return True
                 return elements[i] 
    return False

now = datetime.now()
print (str(now))
result = contains_duplicates([10,0,1,3,-19,5,17,27,37,57,67,77,87,0])
print (str(result))
now = datetime.now()
print (str(now))

